Determine all integers $n \geq 3$ satisfying the following property: every convex $n$-gon whose sides all have length $1$ contains an equilateral triangle of side length $1$.

(Every polygon is assumed to contain its boundary.)
