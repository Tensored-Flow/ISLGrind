% country: NZL
A set of three nonnegative integers $\{x, y, z\}$ with $x < y < z$ is called \emph{historic} if $\{z - y, y - x\} = \{1776, 2001\}$. Show that the set of all nonnegative integers can be written as the union of disjoint historic sets.
